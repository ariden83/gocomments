version: '3.8'

services:
  go-tf-app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - ../runs/checkpoint_model/checkpoint-1:/app/models
    ports:
      - "8080:8080"
    command: ["sh", "-c", "go build -o /bin/myapp . && /bin/myapp"]
